import { CheckIcon } from '@/app/icons'

const ResumeStatusView = () => {
    return (
        <div className='flex gap-2'>
            <CheckIcon />
            <span>Saved</span>
        </div>
    )
}

export default ResumeStatusView